apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.deliveryService.hpa }}
  namespace: {{ .Values.namespace }}
  labels:
    app: delivery-service
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.deliveryService.deploymentname }}
  minReplicas: {{ .Values.deliveryService.minReplicas }}
  maxReplicas: {{ .Values.deliveryService.maxReplicas }}
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
