apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.restaurantService.servicename }}
  namespace: {{ .Values.namespace }}
  labels:
    app: restaurant-service
    app.kubernetes.io/part-of: cloudkitchen
spec:
  type: ClusterIP
  selector:
    app: restaurant-service
  ports:
  - name: http
    port: {{ .Values.restaurantService.port }}
    targetPort: {{ .Values.restaurantService.port }}
