apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.restaurantService.hpa }}
  namespace: {{ .Values.namespace }}
  labels:
    app: restaurant-service
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.restaurantService.deploymentname }}
  minReplicas: {{ .Values.restaurantService.minReplicas }}
  maxReplicas: {{ .Values.restaurantService.maxReplicas }}
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
